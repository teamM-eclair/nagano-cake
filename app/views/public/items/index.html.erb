<h1>商品一覧</h1>

<% @items.each do |item| %>
<div>
  <%= image_tag item.get_image(100,100) %>
  <%= link_to public_item_path(item.id) %>
  <p><%= item.name %></p>
  <p><%= item.unit_price %></p>
</div>
<% end %>

<%= paginate(@item_pages) %>