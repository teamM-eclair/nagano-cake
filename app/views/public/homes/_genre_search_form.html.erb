<table class="table table-bordered">
  <thead>
    <tr>
      <th>ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <% genres.each do |genre| %>
    <tr>
      <td><% genre.name %></td>
        <!-- link_to genre.name, items_path(genre_id: genre.id) -->
    </tr>
    <% end %>
  </tbody>
</table>